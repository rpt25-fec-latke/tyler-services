import React from 'react';
import { shallow } from 'enzyme';

import ReviewFilters from './ReviewFilters.jsx';

describe('It should pass this fake test', () => {
  it('Should pass this fake test', () => {
    expect(1).toEqual(1);
  });
});
